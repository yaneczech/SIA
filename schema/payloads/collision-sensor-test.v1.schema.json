{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/yaneczech/SIA/schema/v0.4/payloads/collision-sensor-test.v1.schema.json",
  "title": "Payload: Interaction.Event.Notification.Diagnostic.CollisionSensorTest (sia:payload:collision-sensor-test:1)",
  "type": "object",
  "required": ["sensor_id", "result"],
  "properties": {
    "sensor_id": { "type": "string", "minLength": 1, "maxLength": 100 },
    "result": { "enum": ["pass", "fail", "degraded"] },
    "detail_code": { "type": "string", "pattern": "^[A-Z][A-Z0-9_]+$", "maxLength": 100 }
  },
  "additionalProperties": false
}
